repeat task.wait() until game:IsLoaded()

-- Supported Games
local SupportedGames = {
    [920587237]  = "https://raw.githubusercontent.com/ValorHub0/Free/main/AdoptmeEVENTS.lua",
    [383310974]  = "https://raw.githubusercontent.com/ValorHub0/Free/main/AdoptmeEVENTS.lua",
    [3647333358] = "https://raw.githubusercontent.com/ValorHub0/Free/main/EvadeGingerbreadfarm.lua",
    [111958650]  = "https://raw.githubusercontent.com/ValorHub0/Free/main/Arsenal.lua",
    [66654135]   = "https://raw.githubusercontent.com/ValorHub0/Free/main/mm2.lua"
}

-- UNIVERSAL HTTP GET (works on Delta, Fluxus, Synapse, etc)
local function HttpGet(url)
    if game.HttpGet then
        return game:HttpGet(url)
    elseif syn and syn.request then
        return syn.request({Url = url, Method = "GET"}).Body
    elseif http and http.request then
        return http.request({Url = url, Method = "GET"}).Body
    end
end

-- Safe loader
local function SafeLoad(url, name)
    local ok, data = pcall(function()
        return HttpGet(url)
    end)

    if not ok or not data or data == "" then
        warn("[ValorHub] Failed loading "..name)
        return
    end

    local func = loadstring(data)
    if not func then
        warn("[ValorHub] Compile error in "..name)
        return
    end

    func()
end

-- Load correct script
local scriptURL = SupportedGames[game.PlaceId] or SupportedGames[game.GameId]

if not scriptURL then
    warn("[ValorHub] Game not supported")
    return
end

SafeLoad(scriptURL, "ValorHub Script")
